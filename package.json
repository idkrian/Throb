{
  "name": "fit",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev -w @fit/api",
    "dev:web": "npm run dev -w @fit/web",
    "build": "npm run build -w @fit/api && npm run build -w @fit/web",
    "build:api": "npm run build -w @fit/api",
    "build:web": "npm run build -w @fit/web",
    "start:api": "npm run start -w @fit/api",
    "lint": "npm run lint -w @fit/web"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
